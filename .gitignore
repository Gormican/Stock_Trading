# ── Sensitive credentials — NEVER commit these ──────────────────────────────
.env
config.json

# ── Runtime files — local only ───────────────────────────────────────────────
gpa_cache.json
sentiment_cache.json
strategies.json

# ── Claude internal working folders — never commit ───────────────────────────
.claude/

# ── Python bytecode ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Streamlit ────────────────────────────────────────────────────────────────
.streamlit/

# ── OS clutter ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
